# xiaozhi-esp32 本地工程使用说明

本文档用于记录新的本地工程目录使用方式。

新的工程目录：

```text
D:\桌面\项目总结\尺蠖机器人\xiaozhi-esp32工程
```

从现在开始，如果你要编译、烧录、查看串口日志，优先进入这个目录，不再进入旧目录：

```text
D:\all_projects\xiaozhi-esp32
```

重要说明：Windows 下 ESP-IDF 和 Git 对中文路径并不总是稳定。本工程虽然放在中文目录下，但建议通过本目录中的批处理脚本进入一个临时英文盘符：

```text
打开ESP-IDF工程命令行.bat
```

运行后，工程会被映射到：

```text
X:\
```

后续编译、烧录、Git 操作都建议在 `X:\` 下执行。

## 1. 这个目录是什么

这是从旧工程复制过来的完整可编译工程，包含：

```text
.git
main
managed_components
sdkconfig
sdkconfig.defaults.esp32s3
dependencies.lock
scripts
components
```

没有复制这些可重新生成的大目录：

```text
build
dist
tmp
releases
.cache
.vscode
.devcontainer
```

所以第一次在这个目录编译时，ESP-IDF 会重新生成新的 `build` 目录。

## 2. 推荐启动方式

推荐方式：直接双击或运行本目录下的：

```text
打开ESP-IDF工程命令行.bat
```

它会自动做三件事：

```text
1. 把当前工程目录映射到 X:\
2. 尝试激活 conda 环境 idf-py311
3. 尝试执行 D:\esp\v5.5.4\esp-idf\export.bat
```

打开后直接执行：

```bat
idf.py build
```

手动方式：如果使用 Anaconda Prompt，并且之前创建过 `idf-py311` 环境：

```bat
conda activate idf-py311
cd /d D:\esp\v5.5.4\esp-idf
export.bat
subst X: /D
subst X: "D:\桌面\项目总结\尺蠖机器人\xiaozhi-esp32工程"
cd /d X:\
idf.py build
```

如果使用 PowerShell，并且 ESP-IDF 环境已经正常：

```powershell
subst X: "D:\桌面\项目总结\尺蠖机器人\xiaozhi-esp32工程"
cd X:\
idf.py build
```

如果 PowerShell 提示 `idf.py` 找不到，说明 ESP-IDF 环境没有初始化，需要先回到 ESP-IDF 目录执行对应的导出脚本。

## 3. 编译

```powershell
cd X:\
idf.py build
```

编译成功后，常用固件文件在：

```text
X:\build\xiaozhi.bin
X:\build\bootloader\bootloader.bin
X:\build\partition_table\partition-table.bin
X:\build\generated_assets.bin
```

如果执行：

```powershell
idf.py merge-bin
```

则会生成合并烧录文件：

```text
X:\build\merged-binary.bin
```

## 4. 烧录

把 `COM口` 换成你的实际串口，例如 `COM4` 或 `COM8`：

```powershell
idf.py -p COM口 flash
```

烧录并直接查看串口：

```powershell
idf.py -p COM口 flash monitor
```

只查看串口：

```powershell
idf.py -p COM口 monitor
```

退出串口监视：

```text
Ctrl + ]
```

## 5. 当前板型

当前项目使用的板型是：

```text
bread-compact-wifi-s3cam
```

核心板级源码：

```text
main\boards\bread-compact-wifi-s3cam\compact_wifi_board_s3cam.cc
```

你之前实现的记忆和 RAG-lite 工具也在这个文件中。

## 6. Git 使用

这个新目录保留了 `.git`，可以继续使用 Git。

查看状态：

```powershell
git status
```

提交修改：

```powershell
git add 修改过的文件
git commit -m "说明这次修改"
git push
```

注意：

```text
sdkconfig、managed_components、build 等目录本来就是被 .gitignore 忽略的。
如果只改 sdkconfig，git status 可能看不到变化。
真正建议提交的是 sdkconfig.defaults.esp32s3、main 下的源码、docs 下的文档。
```

## 7. 和旧目录的关系

旧目录：

```text
D:\all_projects\xiaozhi-esp32
```

新目录：

```text
D:\桌面\项目总结\尺蠖机器人\xiaozhi-esp32工程
```

后续建议：

```text
开发、编译、烧录：使用新目录。
旧目录：只作为历史备份，暂时不要再改。
```
